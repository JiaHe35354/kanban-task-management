import CrossIcon from "@/assets/icon-cross.svg";

import "@/app/globals.css";

function DynamicInputRow({
  value,
  onChange,
  onRemove,
  isInvalid,
  disabled,
  placeholder,
  showRemove = true,
  className = "",
  errorText = "Can't be empty",
}) {
  return <div>DynamicInputRow</div>;
}

export default DynamicInputRow;
